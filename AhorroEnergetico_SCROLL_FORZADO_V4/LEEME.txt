AHORRO ENERGETICO - SCROLL FORZADO V4

Este paquete es distinto a los anteriores:
- modifica DIRECTAMENTE app/page.tsx
- modifica DIRECTAMENTE app/globals.css
- no crea CSS separado
- no depende de imports
- verifica que el cambio realmente haya quedado escrito
- elimina .next para evitar que Next muestre una version vieja

EJECUTAR

1) Cerra npm run dev si esta abierto.

2) Parado en:
   Ahorro-energetico\front

3) Ejecuta:

Set-ExecutionPolicy -Scope Process Bypass
.\AhorroEnergetico_SCROLL_FORZADO_V4\APLICAR_SCROLL_FORZADO_V4.ps1

4) Al final tiene que decir:
   CAMBIO APLICADO Y VERIFICADO

5) Ejecuta:
npm run dev

6) Navegador:
Ctrl + F5

Si el script dice que no encuentra InvoiceTable + MissingInvoiceTable,
manda una captura completa de PowerShell.
