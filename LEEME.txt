FIX V20 METRIC

Error:
ReferenceError: metric is not defined
en TariffSavingTrend.

Causa:
La V20 hizo un reemplazo global de:
const y=...

y agregó por accidente esta línea también dentro del gráfico tarifario:

const graphValue=metric==="pf"&&d.pfUnknownPenalized?0.95:d.value;

TariffSavingTrend no tiene variable `metric`.

Este fix:
- elimina esa línea SOLO dentro de TariffSavingTrend;
- restaura:
  const y=top+plotH-(d.value/max)*plotH;
- no toca InvoiceTrend;
- conserva la nueva lógica de factor de potencia.

Ejecutar:
.\APLICAR_FIX_V20_METRIC.ps1

Luego:
cd front
npm run dev
