import { readFileSync, writeFileSync } from "node:fs";

const path = new URL("../app/invoice-analysis-panel.tsx", import.meta.url);
let source = readFileSync(path, "utf8");
const marker = "// TAX_DB_ONLY_V1";
if (source.includes(marker)) process.exit(0);

source = source.replace(
  '  previous_debt_amount?: number;\n',
  '  previous_debt_amount?: number;\n  vat_rate?: number;\n  vat_perception_amount?: number;\n  municipal_tax_amount?: number;\n  taxes?: unknown;\n',
);

const calcPattern = /  const taxableBase = Number\([\s\S]*?  const taxTotal = taxRows\.reduce\(\(sum, row\) => sum \+ row\.amount, 0\);/;
if (!calcPattern.test(source)) throw new Error("tax calculation block not found");

const calcReplacement = `  // TAX_DB_ONLY_V1\n  // Mostrar únicamente conceptos almacenados explícitamente en la factura.\n  // No inferir percepciones, tasas ni ajustes por diferencia contra el total.\n  const previousDebt = Number(selected.previous_debt_amount || 0);\n  const taxRows = [\n    ...(Number(selected.vat_amount || 0) !== 0\n      ? [\n          {\n            name: "IVA",\n            source: "Registrado en base de datos",\n            base: selected.net_taxable != null ? Number(selected.net_taxable) : null,\n            rate: selected.vat_rate != null ? Number(selected.vat_rate) : null,\n            amount: Number(selected.vat_amount),\n          },\n        ]\n      : []),\n    ...(Number(selected.vat_perception_amount || 0) !== 0\n      ? [\n          {\n            name: "Percepción de IVA",\n            source: "Registrado en base de datos",\n            base: null,\n            rate: null,\n            amount: Number(selected.vat_perception_amount),\n          },\n        ]\n      : []),\n    ...(Number(selected.municipal_tax_amount || 0) !== 0\n      ? [\n          {\n            name: "Tasa municipal",\n            source: "Registrado en base de datos",\n            base: null,\n            rate: null,\n            amount: Number(selected.municipal_tax_amount),\n          },\n        ]\n      : []),\n  ];`;
source = source.replace(calcPattern, calcReplacement);

const sectionPattern = /        <section className="invoice-analysis-panel">\n          <h3>Impuestos y tasas de la factura<\/h3>[\s\S]*?        <\/section>\n\n        \{organizationId && <MeterDocuments/;
if (!sectionPattern.test(source)) throw new Error("tax section not found");

const sectionReplacement = `        <section className="invoice-analysis-panel" style={{ overflow: "hidden" }}>\n          <div style={{ display: "flex", justifyContent: "space-between", gap: 16, alignItems: "flex-start", marginBottom: 14 }}>\n            <div>\n              <span style={{ display: "block", fontSize: 11, fontWeight: 800, letterSpacing: ".08em", color: "#0b8f68", marginBottom: 4 }}>IMPUESTOS REGISTRADOS</span>\n              <h3 style={{ margin: 0 }}>Impuestos y tasas de la factura</h3>\n              <p style={{ margin: "6px 0 0", color: "#6b7a73", fontSize: 13 }}>Se muestran únicamente valores guardados en la base de datos para esta factura.</p>\n            </div>\n            <span style={{ padding: "6px 10px", borderRadius: 999, background: taxRows.length ? "#e9f7f1" : "#f1f3f2", color: taxRows.length ? "#08795a" : "#66736d", fontSize: 12, fontWeight: 800 }}>\n              {taxRows.length ? "Datos disponibles" : "Sin datos registrados"}\n            </span>\n          </div>\n\n          {taxRows.length ? (\n            <div className="invoice-analysis-table-wrap" style={{ border: "1px solid #e2e8e5", borderRadius: 12, overflow: "hidden" }}>\n              <table className="invoice-analysis-table" style={{ margin: 0 }}>\n                <thead>\n                  <tr>\n                    <th>Concepto</th>\n                    <th>Origen</th>\n                    <th>Base imponible</th>\n                    <th>Alícuota</th>\n                    <th style={{ textAlign: "right" }}>Importe</th>\n                  </tr>\n                </thead>\n                <tbody>\n                  {taxRows.map((row) => (\n                    <tr key={row.name}>\n                      <td><b>{row.name}</b></td>\n                      <td><span style={{ display: "inline-block", padding: "4px 8px", borderRadius: 999, background: "#f2f6f4", color: "#53645c", fontSize: 12, fontWeight: 700 }}>{row.source}</span></td>\n                      <td>{row.base != null ? money.format(row.base) : "—"}</td>\n                      <td>{row.rate != null ? \`${"${Number(row.rate).toLocaleString(\"es-AR\", { maximumFractionDigits: 2 })}"}%\` : "—"}</td>\n                      <td style={{ textAlign: "right", fontWeight: 800 }}>{money.format(row.amount)}</td>\n                    </tr>\n                  ))}\n                </tbody>\n              </table>\n            </div>\n          ) : (\n            <div style={{ border: "1px dashed #cfd9d4", borderRadius: 12, padding: "18px 20px", background: "#fafcfb", color: "#65736d" }}>\n              Esta factura no tiene impuestos discriminados cargados en la base. No se calcula ni se completa ningún importe por diferencia.\n            </div>\n          )}\n\n          {previousDebt > 0 && (\n            <p style={{ margin: "12px 0 0", fontSize: 12, color: "#6b7a73" }}>\n              Deuda anterior registrada: <b>{money.format(previousDebt)}</b>. Se muestra aparte porque no corresponde a un impuesto del período.\n            </p>\n          )}\n        </section>\n\n        {organizationId && <MeterDocuments`;
source = source.replace(sectionPattern, sectionReplacement);

writeFileSync(path, source, "utf8");
console.log("Applied DB-only invoice tax display and refreshed tax UI.");
